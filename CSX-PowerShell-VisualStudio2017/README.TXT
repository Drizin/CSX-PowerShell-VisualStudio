VisualStudio2017+ uses the "SDK-style" projects, which use <PackageReference> inside csproj to describe NuGet packages, and use NuGet 4+.

How to run:

1) Build the Project, to restore Nuget references
2) Execute GenerateSimplePOCOs.ps1
